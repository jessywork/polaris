import { ButtonSize, ButtonVariant } from '@typing/components/button';

export const primaryButtonMock = {
  children: 'Primary Button',
  url: '/about',
  variant: ButtonVariant.Primary,
  size: ButtonSize.Medium,
  disabled: false,
};
